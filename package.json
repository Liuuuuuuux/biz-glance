{
  "private": true,
  "name": "biz-glance",
  "version": "0.1.0",
  "packageManager": "pnpm@10.12.4",
  "scripts": {
    "analyze": "pnpm --filter @bizglance/cli dev analyze",
    "serve": "pnpm --filter @bizglance/cli dev serve",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "build": "pnpm --filter @bizglance/web build",
    "smoke": "pnpm --filter @bizglance/cli dev smoke"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
